{
  "name": "awesome-mcp-servers",
  "private": true,
  "workspaces": [
    "servers/*"
  ],
  "scripts": {
    "build": "npm run build -w open-meteo-mcp && npm run build -w rest-countries-mcp && npm run build -w wikipedia-mcp && npm run build -w hacker-news-mcp && npm run build -w unified-mcp"
  }
}
